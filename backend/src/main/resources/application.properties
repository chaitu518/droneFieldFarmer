spring.application.name=drone-field-farmer
server.port=${APP_PORT:8080}

app.cors.allowed-origin=${CORS_ALLOWED_ORIGIN:http://localhost:5173}
app.otp.expires-in-seconds=${OTP_EXPIRES_IN_SECONDS:120}

app.pricing.base-fee=${PRICING_BASE_FEE:300}
app.pricing.travel-fee=${PRICING_TRAVEL_FEE:100}
app.pricing.area-rate=${PRICING_AREA_RATE:250}
